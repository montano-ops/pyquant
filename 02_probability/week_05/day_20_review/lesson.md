# Day 20 — Review: Week 5

Cold retrieval from review.md first. Then the module-integration re-derivations:

1. **The full stack**: E[X] and Var(X) → Var(aX+bY) → portfolio variance →
   SE of the mean → CLT → t-statistic-shaped reasoning. One blank page,
   start to finish, from memory.
2. **The two simulations**: SE(p̂) table (day 10) and best-of-500 noise
   strategies (day 17) — reproduce both from scratch.
3. **The shuffle argument**: what breaks, why, the fix — explain it to an
   imaginary colleague in five sentences.

Tomorrow is the module checkpoint: the win-rate question that pulls the
entire module together. Sleep on it; retrieval works better after a gap.
