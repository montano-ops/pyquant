# Day 8 — Exercise: Covariance & Correlation

## Setup

```python
import os, sys, pathlib
root = pathlib.Path.cwd()
for _ in range(6):
    if (root / "qrc").is_dir():
        break
    root = root.parent
if str(root) not in sys.path:
    sys.path.insert(0, str(root))

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
plt.rcParams["figure.figsize"] = (10, 4)
DATA_SOURCE = os.environ.get("QRC_DATA", "real")
from qrc.data import get_prices
from qrc.synth import synthetic_prices
```

## E1 (L3) — Covariance and ρ by hand

Two series: x = (2, 4, 6, 8, 10), y = (1, 3, 2, 5, 4). Compute Cov and ρ
by hand (center, multiply, average, normalize), then verify with numpy.
What does the sign mean in plain words?

```python
# by hand:
# numpy check:
```

## E2 (L4) — Correlation noise experiment

Draw 2,000 independent 63-day pairs from a normal; record ρ̂ each time.
Report mean, SD (compare 1/√63), and P(|ρ̂|>0.25). Repeat for T=252.
Write the one-sentence implication for "quarterly correlation analysis".

```python
rng = np.random.default_rng(42)
# YOUR CODE
```

## E3 (L5) — Real correlations, with error bars

For [SPY, TLT, GLD, XLE, QQQ] (or synthetic): full-sample correlation
matrix; for each pair, ρ̂ and its SE ≈ (1−ρ̂²)/√T; mark which pairs are
indistinguishable from zero at 2 SE.

```python
# YOUR CODE
```

## E4 (L5) — Rolling correlation instability

Plot the 1-year rolling ρ(SPY, TLT). Report min/max and the fraction of
time its *sign* differs from the full-sample value. What does a single
number "ρ = −0.3" conceal?

```python
# YOUR CODE
```

## E5 (L7) — False conclusion factory

Write a one-paragraph example: a correlation-based hedge (or diversification
claim) that fails exactly when needed, naming the mechanism (regime-
conditional correlation / tail correlation).

```python
# Your answer:
```

## Hints

- E2: `np.corrcoef(x, y)[0, 1]`; SD should land near 1/√63 ≈ 0.126.
- E3: SE formula (1−ρ̂²)/√T is the large-sample SE; 2SE band ≈ 95%.
